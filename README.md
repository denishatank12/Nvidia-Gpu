# HW2.5 — GPU performance measurements

The only notebook retained in this repository is the executed [`HW2_5_GPU_Assignment_5060ti_executed.ipynb`](evidence/rtx5060ti/HW2_5_GPU_Assignment_5060ti_executed.ipynb), which includes its captured outputs. It records an RTX 5060 Ti run and is preserved for review; it is not valid evidence for the assignment's required RTX 4090/5090 hardware.

The directory `evidence/rtx5060ti/` contains the supplied executed 5060 Ti artifacts for provenance and review. They are deliberately kept separate from the submission outputs because an RTX 5060 Ti does not satisfy the lab's RTX 4090/5090 requirement.

## Run on the assigned workstation

The unexecuted template was removed so GitHub displays the executed notebook's outputs. For a valid final submission, rerun an equivalent notebook on the reserved RTX 4090 or RTX 5090 workstation, save the executed file, and replace the 5060 Ti evidence after completing the reservation record and GPU-hour total.

See `SUBMISSION_AUDIT.md` for the requirement-by-requirement status and the exact evidence still needed.

The required Git tag is `hw2-5`.
